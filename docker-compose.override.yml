# Docker Compose Override for Development
# This file overrides settings in docker-compose.yml for local development

version: '3.8'

services:
  # Redis without password for easier development
  redis:
    environment:
      - REDIS_ARGS=  # Empty to disable password
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]  # Remove password from healthcheck